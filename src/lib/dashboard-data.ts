export const salesData: { name: string; sales: number }[] = [];

export const customerData: { name: string; customers: number }[] = [];

export const recentSales: {
  name: string;
  email: string;
  amount: string;
  avatar: string;
  avatarFallback: string;
  imageHint: string;
}[] = [];
